# Model Artifacts

Serialized models are stored here. Common files:

- `Flight Delay Prediction Model.pkl`
- `flight_delay_model.pkl`
- `Flight_Delay_Prediction_Model.pkl`

All binary model files are git-ignored; keep only the lightweight metadata under
`models/metadata/` under version control.
