# Notebook/Tool Exports

Use this directory for generated artifacts such as HTML versions of notebooks,
data dictionary exports, or quick references. Because these can be large and
are reproducible, the folder is ignored by git—commit only the Markdown sources
that describe how to regenerate them.
